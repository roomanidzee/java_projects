spring:
  application:
    name: async-app-zuul

server:
  port: 8082

eureka:

  client:
   serviceUrl:
    defaultZone: http://asyncapp-eureka:9000/eureka

  instance:
    prefer-ip-address: true
    hostname: asyncapp-eureka
